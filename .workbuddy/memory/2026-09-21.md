# 2026-09-21

## 运行 SecWeaver 离线案例（offline showcase）

执行 `make ai-showcase`（等价于 `.venv/bin/python src/scripts/run_ai_showcase.py --all`），覆盖 `examples/ai-showcase/cases.json` 中全部 27 个案例。

### 结果
- **27/27 全部 PASS，0 失败，耗时约 7 秒**
- 每个 case 输出：`outputs/ai-showcase/<case_id>.json` + `<case_id>.md`
- 汇总：`outputs/ai-showcase/suite-summary.json` + `suite-summary.md`
- 案例分布：8 completeness、5 alert、3 traceability、11 risk

### 关键命令
- 单个案例：`make ai-showcase CASE=<case_id>` 或 `.venv/bin/python src/scripts/run_ai_showcase.py <case_id>`
- 全部：`make ai-showcase` / `--all`
- 列出可用 case：`--list`
- 每 case 60 秒超时；离线模式（`offline=true`，禁止 `--fetch`/`--notify`/`--no-ip-intel` 在 traceability 下必现）

### 环境注意
- `.venv` 已存在（Python 3.14.6，pyvenv.cfg 指向 `/opt/homebrew/bin/python3` 系统解释器）
- `requirements-data-access.txt` 已安装
- 无网络即可运行