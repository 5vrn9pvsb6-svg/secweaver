{
  "template_id": "YOUR_TEMPLATE_ID",
  "asset_types": ["waf_alert"],
  "connector_types": ["sls_proxy"],
  "params": ["src_ip", "time_start", "time_end", "limit"],
  "defaults": { "limit": 2000 },
  "description": "通过 SecWeaver SLS Proxy 按源 IP + 时间窗查询",
  "sls_query": "src_ip: {src_ip} OR ip: {src_ip} | select * limit {limit}"
}
