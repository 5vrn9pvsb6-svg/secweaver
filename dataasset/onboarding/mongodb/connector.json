{
  "connector_id": "YOUR_CONN_ID",
  "name": "MongoDB 只读（模板）",
  "connector_type": "mongodb",
  "status": "draft",
  "credentials_ref": "vault://mongodb/security-readonly",
  "description": "MongoDB 只读查询模板；live fetch 需要 pymongo。",
  "config": {
    "host": "mongodb.example.com",
    "port": 27017,
    "database": "security_logs",
    "collection": "events",
    "auth_source": "admin"
  },
  "constraints": {
    "max_records_per_request": 1000,
    "request_timeout_sec": 30
  },
  "tags": [
    "template",
    "mongodb",
    "database"
  ]
}
