{
  "template_id": "YOUR_TEMPLATE_ID",
  "asset_types": [
    "waf_alert"
  ],
  "connector_types": [
    "azure_monitor"
  ],
  "params": [
    "src_ip",
    "time_start",
    "time_end",
    "limit"
  ],
  "defaults": {
    "limit": 1000
  },
  "kql": "SecurityEvent | where TimeGenerated between (datetime({time_start}) .. datetime({time_end})) | where IpAddress == '{src_ip}' | take {limit}"
}
