# 阿里云 SLS 只读 RAM 凭证 - 编辑后执行: DATAASSET_ROOT=dataasset src/dataasset/credentials/sops-vault.sh edit vault://sls/security-readonly
type: aliyun_ram
access_key_id: "LTAI_REPLACE_ME"
access_key_secret: "REPLACE_ME"
# security_token: ""  # STS 临时凭证时填写
